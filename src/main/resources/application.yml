spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/serverdb
    password: postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
        format_sql: true
    show-sql: true